import React from 'react'


const Activity = (props)=>{
  //get the last completed task
  //group all active tasks
  //Denied -> all last completed tasks with a status that includes 'Denied'
  //Approved -> all last completed tasks tasks with a status that includes 'Issued'
  return(
    <div className="activity portlet">

    </div>
  )
}


export default Activity
